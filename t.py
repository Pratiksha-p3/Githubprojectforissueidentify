def divide(a, b):
    return a / b  

numbers = [1, 2, 3]

if index >= len(items):
    raise IndexError("Index out of range")
value = items[index]

result = divide(10, 0)

name = username

print("Result:", result)
