# ── LLM (Free) ───────────────────────────────────────────
groq

# ── Embeddings (Free local) ───────────────────────────────
sentence-transformers

# ── Vector DB ─────────────────────────────────────────────
chromadb==0.5.3

# ── LangGraph multi-agent ─────────────────────────────────
langgraph
langchain-core

# ── Security scanning ─────────────────────────────────────

bandit

# ── GitHub integration ────────────────────────────────────
requests==2.32.3
PyJWT==2.8.0
cryptography==42.0.8

# ── Web framework + dashboard ─────────────────────────────
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.4
aiofiles==23.2.1
httpx==0.27.0

# ── Config ────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Utilities ─────────────────────────────────────────────
rich==13.7.1

# ── Optional paid LLM providers (LLM_PROVIDER=openai|anthropic) ──
openai>=1.54,<2
anthropic>=0.34,<1

# ── Optional infra — all no-op/fall back to the free defaults above
#    unless their env vars are set. See config.py for the switches. ──
pinecone>=5.0,<6            # VECTOR_DB_PROVIDER=pinecone
sqlalchemy>=2.0,<3           # DATABASE_URL (Postgres report persistence)
psycopg2-binary>=2.9,<3      # DATABASE_URL
redis>=5.0,<6                # REDIS_URL (LLM response cache)
temporalio>=1.7,<2           # TEMPORAL_ADDRESS (pipeline orchestration)